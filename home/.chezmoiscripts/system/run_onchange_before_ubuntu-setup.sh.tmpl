{{- if eq .osid "linux-ubuntu" -}}
#!/usr/bin/env bash

echo 'Installing packages...'
sudo apt-get update
sudo apt-get install -y {{ join " " (.packages.ubuntu.apt | sortAlpha | uniq) }}

echo 'Setting up zsh...'
sudo chsh -s $(which zsh) ${USER}

{{- end -}}
